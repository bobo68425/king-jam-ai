fastapi==0.115.0
uvicorn[standard]==0.34.0
pydantic>=2.5.0
httpx>=0.27.0

# ── PyTorch (需 CUDA 12.7+) ─────────────────────────────────────
# 若使用 Docker/Cloud Run, 基底映像需包含 CUDA 12.8+ runtime
# torch>=2.7.0 已由基底映像提供; 若需手動安裝:
# pip install torch>=2.7.0 --index-url https://download.pytorch.org/whl/cu128

# ── LTX-2.3 核心套件 (從 Lightricks 官方 repo 安裝) ──────────────
# git clone https://github.com/Lightricks/LTX-2.git
# cd LTX-2 && pip install packages/ltx-core packages/ltx-pipelines

# ── Hugging Face / Transformers ──────────────────────────────────
transformers>=4.51.0
accelerate>=1.6.0
safetensors>=0.4.0
sentencepiece>=0.2.0
huggingface_hub>=0.28.0

# ── Video / Image / Audio utilities ─────────────────────────────
imageio-ffmpeg==0.5.1
imageio>=2.36.0
Pillow>=11.0.0
opencv-python-headless>=4.10.0
soundfile>=0.13.0

# ── Cloud Storage ────────────────────────────────────────────────
boto3>=1.34.0

# NOTE: torch, torchvision, torchaudio 需 >= 2.7.0 (CUDA 12.7+)
# LTX-2.3 需要 Python >= 3.12
# 不要在此安裝 torch 以避免與基底映像衝突
