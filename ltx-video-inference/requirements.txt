fastapi==0.109.0
uvicorn[standard]==0.27.0.post1
pydantic==2.5.3
httpx==0.27.0

# ── Hugging Face / Diffusers ─────────────────────────────────────
diffusers==0.32.1
transformers==4.47.1
accelerate==1.2.1
peft==0.14.0
sentencepiece==0.2.0

# ── Video / Image utilities ──────────────────────────────────────
imageio-ffmpeg==0.5.1
imageio==2.36.1
Pillow==11.0.0
opencv-python-headless==4.10.0.84

# ── Quantization / FP8 (accelerates attention layers on L4 Lovelace) ────────
# bitsandbytes: INT8/FP8 quantization for transformer attention blocks
bitsandbytes==0.44.1

# NOTE: torch, torchvision, torchaudio are already in the base image
# (pytorch/pytorch:2.5.1-cuda12.4-cudnn9-runtime)
# Do NOT add them here to avoid version conflicts.
